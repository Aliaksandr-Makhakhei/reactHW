import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import CheckBox from './components/emoji-states';

ReactDOM.render(
  <React.StrictMode>
    <CheckBox />
  </React.StrictMode>,
  document.getElementById('root')
);