import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import CyclicButton from './component/cyclicButton/';

ReactDOM.render(
  <React.StrictMode>
    <CyclicButton />
  </React.StrictMode>,
  document.getElementById('root')
);